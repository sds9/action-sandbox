name: variables

on:
  push:
  # workflow_dispatch:
  #   inputs:
  #     environment:
  #       description: 'Environment to deploy to'
  #       required: true
  #       type: choice
  #       options:
  #         - dev
  #         - test
  #         - prod

# env:
#   ACCOUNT_NUMBER: ${{ inputs.environment == 'dev' && '123456789012' || inputs.environment == 'test' && '111111111111' || '210987654321' }}
#   REGION: ${{ inputs.environment == 'dev' && 'us-west-2' || inputs.environment == 'test' && 'us-central-1' || 'us-east-1' }}
#   BUCKET: ${{ inputs.environment == 'dev' && 'my-dev-bucket' || inputs.environment == 'test' && 'my-test-bucket' || 'my-prod-bucket' }}

jobs:
  # start:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Display environment variables
  #       run: |
  #         echo "Environment: ${{ inputs.environment }}"
  #         echo "Account number: $ACCOUNT_NUMBER"
  #         echo "Region: $REGION"
  #         echo "Bucket: $BUCKET"

  #     - name: Example step using environment variables
  #       run: |
  #         echo "Deploying to account $ACCOUNT_NUMBER in region $REGION using bucket $BUCKET"
      

  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Set multiline output
        id: set-output
        run: |
          echo "multiline-output<<EOF" >> $GITHUB_OUTPUT
          echo "Line 1" >> $GITHUB_OUTPUT
          echo "Line 2" >> $GITHUB_OUTPUT
          echo "Line 3" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "${multiline-output}"

      - name: Use multiline output
        run: |
          echo "Multiline output:"
          echo "${{ steps.set-output.outputs.multiline-output }}"